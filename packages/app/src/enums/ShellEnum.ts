export enum ShellEnum {
    CAT = 'cat',
    TOUCH = 'touch',
    LS = 'ls',
    WHICH = 'which',
    ECHO = 'echo',
    GREP = 'grep',
    CP = 'cp',
    BASH = 'bash',
    MV = 'mv',
    SERVICE = 'service',
    SYSTEMCTL = 'systemctl'
}

export enum ServiceEnum {
    STATUS = 'status',
    START = 'start',
    RESTARt = 'restart',
    STOP = 'stop'
}
